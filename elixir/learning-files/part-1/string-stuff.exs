IO.puts "Hello #{:world}"

IO.puts "Hello\nWorld!"

String.length("rawr ecks dee")

String.upcase("all lower case")

# Single quotations are not strings, but rather integers in a list/array where the letters are changed to the integer value
